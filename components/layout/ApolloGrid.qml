import QtQuick.Layouts
import "../../theme" as Tokens

GridLayout {
    columns: 2
    rowSpacing: Tokens.Theme.scaled(Tokens.Theme.spacing.itemGap)
    columnSpacing: Tokens.Theme.scaled(Tokens.Theme.spacing.itemGap)
}
